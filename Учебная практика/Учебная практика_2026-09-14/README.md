# Скидки партнеров: бизнес-логика, база и интерфейс

Учебная практика 14.09.2026. Python 3.14, PySide6, PostgreSQL, psycopg 3.

## Задания

| Задание | Папка | Что внутри |
|---|---|---|
| 1. Расчет скидки | [Разработка ядра бизнес-логики (Расчет скидки)](<./Разработка ядра бизнес-логики (Расчет скидки)>) | функция расчета и тесты границ |
| 2. Интеграция с БД | [Интеграция с БД и агрегация данных (SQL + Backend)](<./Интеграция с БД и агрегация данных (SQL + Backend)>) | схема, тестовые данные, запросы |
| 3. Интерфейс | [Разработка интерфейса (UI) по руководству по стилю](<./Разработка интерфейса (UI) по руководству по стилю>) | окно на PySide6, ресурсы, снимок |
| 4. Наполнение и отладка | [Наполнение интерфейса, отладка и стресс-тестирование](<./Наполнение интерфейса, отладка и стресс-тестирование>) | точка входа, тесты, снимок прогона |

У каждого задания свой README с ответом. Задания здесь - этапы одной
программы, и модули ссылаются друг на друга. Папки с русскими именами и
пробелами пакетами Python быть не могут, поэтому модули соседних заданий
подключаются через пути поиска: это делают `conftest.py` и `main.py`.

## Запуск

Зависимости:

```
pip install -r requirements.txt
```

База разворачивается по [заданию 2](<./Интеграция с БД и агрегация данных (SQL + Backend)>): создать
`partner_discounts`, выполнить `schema.sql`, затем `seed.sql`.

Приложение, из этой папки:

```
$env:PGPASSWORD = "пароль"
python "Наполнение интерфейса, отладка и стресс-тестирование/main.py"
```

Тесты с замером покрытия:

```
python -m coverage run -m pytest
python -m coverage report
```

Если база недоступна, тесты задания 2 и сквозные пропускаются, остальные идут
как обычно.

## Итог

29 тестов, покрытие всех модулей - 100%. Скидки на тестовых данных совпадают с
границами из ТЗ, партнер без истории продаж получает 0%, ошибка базы не роняет
окно. Подробности - в README заданий.
